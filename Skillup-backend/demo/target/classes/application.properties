spring.application.name=skillup-backend

# ============================================
# BASE DE DATOS - PostgreSQL Local
# ============================================
spring.datasource.url=jdbc:postgresql://localhost:5432/skillup_db
spring.datasource.username=postgres
spring.datasource.password=Tics2030
spring.datasource.driver-class-name=org.postgresql.Driver

# ============================================
# JPA / HIBERNATE
# ============================================
# IMPORTANTE: "none" porque ya creamos las tablas manualmente
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ============================================
# SERVER
# ============================================
server.port=8080

# ============================================
# JWT
# ============================================
jwt.secret=skillup_secret_key_super_largo_para_desarrollo_local_2025_123456789
jwt.expiration=86400000

# ============================================
# CORS (Frontend local)
# ============================================
cors.allowed.origins=http://localhost:5500,http://127.0.0.1:5500

# ============================================
# LOGGING
# ============================================
logging.level.com.skillup.backend=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG